#ifndef LSL_LOGGING_H
#define LSL_LOGGING_H

#define LslError(msg) do {} while(0)
#define LslDebug(msg) do {} while(0)
#define LslWarning(msg) do {} while(0)
#define LslDebug(msg) do {} while(0)

#endif // LSL_LOGGING_H
